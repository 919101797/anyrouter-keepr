# 更新日志

## 0.4.0 - 2026-07-02

### 新增

- 新增 AnyRouter 指纹面板，可查看本机 Claude 指纹、历史记录和代理状态。
- 新增指纹代理，支持重写 `X-Stainless-OS` / `X-Stainless-Arch`，并剥离 `context_management`。
- 支持一键切换 OS、Arch 和 `device_id`，并自动保存、恢复、删除指纹历史。
- 探测历史新增指纹列，便于回溯每次 probe 使用的有效 OS、Arch、device_id 和代理来源。

### 改进

- 代理启动后自动同步 Claude / cc-switch 路由，停止代理时恢复真实上游地址。
- 指纹与代理状态接入前端状态管理，路由同步失败会直接展示给用户。
- 新增代理、指纹、路由同步和数据库迁移相关回归测试。

### 修复

- 修复代理端口启动失败或路由同步失败时，仍可能改写本机 `device_id` 的问题。
- 修复停止代理后 Claude 配置仍指向本地 keeper 代理的问题。

## 0.3.0 - 2026-06-29

### 修复

- 修复 macOS 端探测过程中点击“停止探测”后界面长时间卡在 loading、按钮置灰且状态不明确的问题。
- 修复“探测一次”和“停止探测”共用 loading 状态导致两个按钮同时表现为不可用的问题。
- 修复自定义 Claude CLI 路径无法独立测试、保存入口不明显的问题。
- 支持在 Claude CLI 路径中填写目录，例如 `/usr/local/bin`，应用会自动解析到目录内的 `claude` 可执行文件。

### 改进

- 在“Claude 可执行文件路径”输入框下方新增“识别当前路径”和“保存路径”两个明确操作按钮。
- 为守护操作、探测、Claude 路径识别、配置保存拆分独立 pending 状态，避免错误 loading 反馈。
- 增加 Claude 路径识别和探测停止流程的回归测试。
